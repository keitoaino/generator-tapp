Description:
    Creates a new basic front-end web application with Jade, Stylus and CoffeeScript.

Options:
  CreateJS: Include set of libraries (EaselJs, TweenJs, SoundJS, PreloadJS)

Example:
    yo tapp

    This will create:
        gulpfile.js: Configuration for the task runner.
        bower.json: Front-end packages installed by bower.
        package.json: Development packages installed by npm.

        app/: Your application files.
        test/: Unit tests for your application.
